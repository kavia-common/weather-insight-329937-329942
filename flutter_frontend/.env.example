# Flutter Frontend Environment Variables
#
# This project loads environment variables from `.env` (included as a Flutter asset).
# Do NOT commit secrets.
#
# Backend URL for Weather Insight FastAPI (example)
BACKEND_BASE_URL=https://your-backend-host:3001

# Default city used if no stored "last searched city" exists
DEFAULT_CITY=London
